## Basic prompts

1. Keep prompt templates in a shared repo
2. Define project-specific file groups in a JSON file
3. Run a small script
4. Script reads the JSON, concatenates files, replaces {{include:name}} placeholders, outputs final prompt to clipboard